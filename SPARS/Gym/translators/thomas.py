# SPARS/Gym/translators/scalar_active_target.py
# NOTE: Logic unchanged; comments added only.
import logging
import torch as T

logger = logging.getLogger("runner")


def action_translator(logits):
    print(logits)

    return
